# Editorial Review — Prose

**Source:** `_bmad-output/planning-artifacts/prds/prd-web-2026-05-25/prd.md`
**Reader type:** `llm` (priority: unambiguous references, consistent terminology, explicit structure, no hedging)
**Style baseline:** Microsoft Writing Style Guide
**Notes:** §5.0 Voice & Tone preserved as authorial voice (not edited toward neutral). Frontmatter, code blocks, and table separator rows skipped.

| Original Text | Revised Text | Changes |
|---|---|---|
| The site is a **public signaling artifact** built before an active job hunt — it must work for both recruiters (10-second scan → "interview yes/no") and engineering managers (deep technical credibility check → "can he build what I need?"). | The site is a **public signaling artifact** built before an active job hunt — it must work for both recruiters (10-second scan → "interview yes/no") and engineering managers (deep technical credibility check → "can this person build what I need?"). | Replaced gendered pronoun "he" with "this person" for inclusive, neutral phrasing in a quoted reviewer voice. |
| Spends 30–90 seconds per portfolio before deciding "screen / pass." | Spends 30–90 seconds per portfolio before deciding "screen or pass." | Replaced the slash with "or" for unambiguous parsing; slashes between verbs read as either/and to an LLM consumer. |
| **Wants:** clear title, years of experience, current/last company, tech-stack match against the open role, links to case studies (does not read them), a way to download the resume PDF or copy contact, a "is this person senior?" gut-feel signal. | **Wants:** clear title, years of experience, current or last company, tech-stack match against the open role, links to case studies (does not read them), a way to download the resume PDF or copy contact, and a "is this person senior?" gut-feel signal. | Replaced "current/last" slash with "or"; added Oxford "and" before the final list item to make the enumeration boundary explicit. |
| **Does not want:** to learn a new UI, to scroll a long story, to play games, to figure out where contact info is. | **Does not want:** to learn a new UI, to scroll a long story, to play games, or to figure out where contact info is. | Added "or" before the final list item to mark the enumeration boundary unambiguously. |
| **Wants:** case studies with the *why*, not just the *what*. Tech-stack honesty (what you actually used vs. what was on the team). Failed-or-archived projects flagged honestly. Interaction craft visible in the artifact. Reasonable performance — pages don't jank, animations don't gratuitously block content. | **Wants:** case studies with the *why*, not just the *what*; tech-stack honesty (what you actually used vs. what was on the team); failed-or-archived projects flagged honestly; interaction craft visible in the artifact; and reasonable performance — pages don't jank, animations don't gratuitously block content. | Converted period-separated fragments into a semicolon-delimited list under the "Wants:" colon for consistent parsing with the parallel P1 "Wants:" entry. |
| **Does not want:** marketing copy ("passionate developer"), tutorial-finishing screenshots labeled as "projects," skill bars/percentages, polished-but-empty "experimental" features. | **Does not want:** marketing copy ("passionate developer"), tutorial-finishing screenshots labeled as "projects," skill bars or percentages, or polished-but-empty "experimental" features. | Replaced "skill bars/percentages" slash with "or"; added final "or" for list-boundary clarity. |
| **Wants:** craft details — the Konami code, the REPL commands, the waterfall metaphor done right, the type system on display. | **Wants:** craft details — the Konami code, the REPL commands, the waterfall metaphor done right, and the type system on display. | Added Oxford "and" before the final list item for explicit enumeration boundary. |
| Seven routes, all under a persistent DevTools chrome (IA-C: chrome stays mounted across tab switches; route transitions animate, do not full-reload). | Seven routes, all under a persistent DevTools chrome (IA-C: the chrome stays mounted across tab switches; route transitions animate and do not trigger a full reload). | Added definite article before "chrome" to disambiguate from the Chrome browser; clarified "do not full-reload" (verb-as-noun construction) to "do not trigger a full reload." |
| **Mobile (`<640px`)** chrome collapses to a bottom tab bar with safe-area-inset-bottom respected. | **Mobile (`<640px`)** chrome collapses to a bottom tab bar, with `safe-area-inset-bottom` respected. | Added comma before the trailing absolute phrase; backticked the CSS env-var token for consistency with other code references. |
| Each UJ names a persona by exact label from §3 and walks a representative session. | Each UJ names a persona by its exact label from §3 and walks through a representative session. | Added possessive "its" for explicit reference; changed "walks a session" to the idiomatic "walks through a session." |
| Tab closes. Total time on site: 30–60 seconds. Outcome: pass-to-EM decision made. | The tab closes. Total time on site: 30–60 seconds. Outcome: pass-to-EM decision made. | Added article "The" — bare "Tab closes" is telegraphic and ambiguous as to whether "Tab" is the keyboard key or the browser tab. |
| **Success:** Resume PDF download fires within 60 seconds of landing. **Failure:** P1 cannot find Recruiter Mode within 10 seconds and bounces, or P1 finds the chrome alienating and bounces before toggling. | **Success:** the Resume PDF download fires within 60 seconds of landing. **Failure:** P1 cannot find Recruiter Mode within 10 seconds and bounces, or P1 finds the chrome alienating and bounces before toggling. | Added "the" article for parallel structure with the Failure clause and to avoid headline-style telegraphic phrasing. |
| Clicks the **Network** tab. Sees the waterfall of all 22 projects. Filters by `status: shipped` to remove ongoing/archived noise. | Clicks the **Network** tab. Sees the waterfall of all 22 projects. Filters by `status: shipped` to remove ongoing or archived noise. | Replaced "ongoing/archived" slash with "or" — the slash here reads ambiguously as conjunction. |
| Notes the `stack` chips, clicks the "live" link in a new tab, returns. | Notes the `stack` chips, clicks the "live" link to open it in a new tab, then returns. | Clarified that "in a new tab" describes the click's behavior, not the subject's location; added "then" before "returns" for explicit temporal sequencing. |
| Returns to LinkedIn / Slack with a screening signal. | Returns to LinkedIn or Slack with a screening signal. | Replaced slash with "or" for unambiguous logical relation. |
| **Success:** P2 reads ≥2 case studies and finds at least one "Decisions" entry that articulates a real trade-off (not marketing). **Failure:** every case study reads as feature-list summary with no trade-off rationale. | **Success:** P2 reads ≥2 case studies and finds at least one "Decisions" entry that articulates a real trade-off (not marketing). **Failure:** every case study reads as a feature-list summary with no trade-off rationale. | Added missing article "a" before "feature-list summary." |
| Lands on `/` from a Twitter/HN share. | Lands on `/` from a Twitter or HN share. | Replaced slash with "or" for unambiguous logical relation. |
| Tries the Konami sequence (`↑↑↓↓←→←→BA`) somewhere on the site. | Tries the Konami sequence (`↑↑↓↓←→←→BA`) somewhere on the site. (Consider: "anywhere on the site"?) | Query: "somewhere" is vague; "anywhere" more precisely conveys that the sequence works regardless of location. Flagged rather than changed because intent is plausibly deliberate. |
| **Failure:** REPL commands feel canned, Konami unlock leads to a placeholder, P3 leaves disappointed. | **Failure:** REPL commands feel canned, the Konami unlock leads to a placeholder, or P3 leaves disappointed. | Added article "the" before "Konami unlock" and "or" before the final clause to mark the disjunction explicitly (these are alternative failure modes, not a sequence). |
| The PRD compresses interaction craft into FR tables, but the **feel** of devtools://hossam is load-bearing and must survive the compression. These voice rules are non-negotiable in implementation. | _(no change — §5.0 voice preserved per project rule)_ | §5.0 authorial voice preserved per style note; not edited. |
| Implementation rule: when an FR conflicts with a voice rule above, raise it — voice wins unless there's a hard accessibility or performance reason. | Implementation rule: when an FR conflicts with a voice rule above, raise the conflict — voice wins unless there is a hard accessibility or performance reason. | Replaced pronoun "it" with explicit antecedent "the conflict" (the nearest noun could be "rule" or "FR"); expanded contraction "there's" to "there is" for LLM-targeted formal register. |
| Chrome renders on every route except `/recruiter`. | The chrome renders on every route except `/recruiter`. | Added definite article "The" — bare "Chrome" risks parsing as the Chrome browser; the PRD's own Glossary entry warns of this confusion. |
| Tab switches animate via `motion/react` `AnimatePresence mode="wait"` between page sections; chrome itself does not re-mount. | Tab switches animate via `motion/react` `AnimatePresence mode="wait"` between page sections; the chrome itself does not re-mount. | Added definite article for chrome-vs-Chrome disambiguation (same reason as FR-001 fix). |
| Mobile chrome: bottom tab bar replaces the desktop tab row. `env(safe-area-inset-bottom)` respected. Identity strip stays at top single-row. | Mobile chrome: a bottom tab bar replaces the desktop tab row. `env(safe-area-inset-bottom)` is respected. The identity strip stays as a single row at the top. | Added articles; supplied verb "is" in the elliptical sentence; rephrased "at top single-row" (telegraphic) into a proper prepositional phrase. |
| The HTTP method is **decorative metaphor, not semantic typing** — chosen for the waterfall joke (a project list rendered as a network panel). | The HTTP method is a **decorative metaphor, not semantic typing** — chosen for the waterfall joke (a project list rendered as a network panel). | Added missing indefinite article "a" before "decorative metaphor." |
| A loose rule of thumb is used during content authoring (GET = ongoing product work, POST = new launches, PUT = rewrites/refactors, PATCH = small / contract work), but no FR depends on the mapping being interpreted literally. | A loose rule of thumb is used during content authoring (GET = ongoing product work, POST = new launches, PUT = rewrites or refactors, PATCH = small or contract work), but no FR depends on the mapping being interpreted literally. | Replaced "rewrites/refactors" and "small / contract work" slashes with "or" for explicit disjunction. |
| If a reviewer reads the column and thinks "that's not what HTTP methods mean," that is the intended reading. | If a reviewer reads the column and thinks "that is not what HTTP methods mean," that is the intended reading. | Expanded contraction "that's" inside the quoted thought to match the surrounding formal register and the unexpanded "that is" at the end of the sentence. (Optional — keep "that's" if internal monologue voice is intentional.) |
| Color: `--status-ok` / `--status-warn` / `--status-err` from tokens. | Color tokens: `--status-ok`, `--status-warn`, `--status-err`. | Replaced slash-separated enumeration with comma-separated list; tightened "Color:" to "Color tokens:" so the colon's referent is unambiguous; removed trailing "from tokens" (now redundant). |
| Size column renders the `size` label (e.g., "12.4 MB", "894 KB") — the bytes are a metaphor for project impact/scope, not actual page weight. | Size column renders the `size` label (e.g., "12.4 MB", "894 KB") — the bytes are a metaphor for project impact or scope, not actual page weight. | Replaced "impact/scope" slash with "or" for unambiguous disjunction. |
| Bars use `transform: scaleX()` only, never `width` (perf rule). | Bars use `transform: scaleX()` only, never `width` (performance rule). | Expanded "perf" to "performance" in prose context (the abbreviation is fine in IDs like NFR-P1 but inconsistent in body copy where the full word is used elsewhere). |
| **FR-026.** Filter chips at top of `/work`: by `method`, by `status`, by `year`. Multi-select. URL-persisted via search params for shareable filtered views. | **FR-026.** Filter chips at the top of `/work`: by `method`, by `status`, by `year`. Multi-select. Persisted to the URL via search params for shareable filtered views. | Added "the" before "top"; rephrased "URL-persisted via search params" (compound-adjective-as-verb) to the more direct "Persisted to the URL via search params." |
| Case study route is statically generated via `generateStaticParams` from the content file. | The case-study route is statically generated via `generateStaticParams` from the content file. | Added article "The"; hyphenated "case-study" as a compound modifier for consistency with other case-study compound modifiers in the PRD. |
| Layout: top breadcrumb (`Network > [project name]`), then case study sections in order: **Problem** (1–3 paragraphs) → **Role** (1 paragraph, what *you* specifically did vs. the team) → **Stack** (chips from `stack[]`) → **Decisions** (bulleted, each with brief rationale) → **Outcomes** (bulleted, ideally with before/after numbers) → **Links** (live, code, design — filtered to non-null). | Layout: top breadcrumb (`Network > [project name]`), then case-study sections in order: **Problem** (1–3 paragraphs) → **Role** (1 paragraph, what *you* specifically did vs. the team) → **Stack** (chips from `stack[]`) → **Decisions** (bulleted, each with a brief rationale) → **Outcomes** (bulleted, ideally with before-and-after numbers) → **Links** (live, code, design — filtered to non-null). | Hyphenated "case-study" as compound modifier; added missing article "a" before "brief rationale"; replaced "before/after" slash with "before-and-after" hyphenated compound for unambiguous parsing. |
| **FR-034.** v1 case-study scope: **6 case studies** authored fully, remaining projects listed in the waterfall but link to the source repo / live URL only with no detail page. | **FR-034.** v1 case-study scope: **6 case studies** authored fully; remaining projects are listed in the waterfall but link to the source repo or live URL only, with no detail page. | Replaced the comma splice with a semicolon; supplied verb "are"; replaced "source repo / live URL" slash with "or"; added comma before the trailing absolute phrase. |
| `/console` renders a terminal-style REPL backed by a real `<input>` (a11y: real input, not contenteditable). History buffer navigable with ↑/↓ arrows. | `/console` renders a terminal-style REPL backed by a real `<input>` (a11y: real input, not `contenteditable`). The history buffer is navigable with ↑/↓ arrows. | Backticked `contenteditable` for consistency with how the attribute is rendered elsewhere (FR-081); added article and verb to fragment sentence. |
| Command outputs: | Command outputs are listed below. | Made the colon-ending phrase a complete sentence to introduce the bullet list — bare "Command outputs:" reads as a heading rather than a sentence, and LLM consumers parse the introducer more reliably as a clause. (Optional — bullet-intro style is consistent elsewhere; preserve if intentional.) |
| `whoami` → renders `Profile.name`, `role`, `location`, `email`, `years`. | `whoami` → renders `Profile.name`, `Profile.role`, `Profile.location`, `Profile.email`, and `Profile.years`. | Disambiguated the dotted-property references — without the `Profile.` prefix on each, `role`/`location`/etc. are ambiguous global identifiers; also added Oxford "and." |
| `theme` → `theme dark` / `theme light` (light errors with "Site is dark-only. The vibe is intentional."). | `theme` → accepts `theme dark` and `theme light` (the `theme light` invocation errors with "Site is dark-only. The vibe is intentional."). | Replaced slash with explicit conjunction; expanded elided subject "light errors" to "the `theme light` invocation errors" — "light errors" alone parses ambiguously as a noun phrase. |
| Each successful command grants +5 XP (FR-074). | Each successful REPL command grants +5 XP (FR-074). | Disambiguated "command" — between FR-042 (REPL command outputs) and FR-091 (⌘K palette commands), "command" without qualifier is ambiguous. The Konami section uses "REPL command" explicitly for the same reason. |
| Pasting multiline strings is supported but only the first line executes (rest is ignored with a notice). | Pasting multiline strings is supported, but only the first line executes (the remaining lines are ignored with a notice). | Added comma before "but" (independent clauses); replaced bare "rest" (informal, ambiguous antecedent) with explicit "the remaining lines." |
| Renders score rings (à la Lighthouse) for: **years shipped**, **projects shipped**, **talks/articles**, **mentees** [ASSUMPTION: numbers from `Profile.metrics[]`; if mentees/talks are zero, those rings are omitted, not zeroed]. | Renders score rings (à la Lighthouse) for **years shipped**, **projects shipped**, **talks and articles**, and **mentees** [ASSUMPTION: numbers come from `Profile.metrics[]`; if `mentees` or `talks` are zero, those rings are omitted, not zeroed]. | Removed the unnecessary colon after "for" (the list is grammatically the object of "for"); replaced "talks/articles" and "mentees/talks" slashes with explicit conjunctions; supplied verb "come" for the elided clause inside the ASSUMPTION; backticked the field references. |
| Below rings: a "page weight budget" visualization showing the actual site's bundle composition (HTML / JS / CSS / images / fonts). | Below the rings: a "page weight budget" visualization showing the actual site's bundle composition (HTML, JS, CSS, images, fonts). | Added article "the"; replaced slash-separated enumeration with comma-separated list (a bundle is composed of all categories, not one-of). |
| Left pane (desktop `≥md`): file tree with entries `resume.pdf`, `articles/`, `talks/`, `contact.ts`. | Left pane (desktop `≥md`): file tree with entries `resume.pdf`, `articles/`, `talks/`, and `contact.ts`. | Added Oxford "and" for explicit enumeration boundary. |
| Right pane: preview of selected file. | Right pane: preview of the selected file. | Added article "the" — bare "selected file" reads ambiguously (any selected file vs. the currently selected one). |
| Form lives under `/sources` `contact.ts`. UI is a typed-terminal sequence: each field prompts inline, validates as you type, renders validation result as a passing test (`✓ email format`, `✓ message length ≥ 20 chars`). | The form lives under `/sources` `contact.ts`. The UI is a typed-terminal sequence: each field prompts inline, validates as you type, and renders the validation result as a passing test (`✓ email format`, `✓ message length ≥ 20 chars`). | Added articles "The" / "the"; added Oxford "and" before the third clause in the serial verb chain; added article before "validation result." |
| Fields: `name` (required, ≥2 chars), `email` (required, RFC-compliant), `subject` (optional, ≤120 chars), `message` (required, ≥20 chars, ≤2000 chars). | Fields: `name` (required, ≥2 chars); `email` (required, RFC-compliant); `subject` (optional, ≤120 chars); `message` (required, ≥20 chars, ≤2000 chars). | Replaced top-level commas with semicolons because the per-field constraints already use commas internally — the semicolons mark the field-level boundary unambiguously. |
| Submit: stubbed in v1. Returns faked success after 600–1200ms (randomized). | Submit: stubbed in v1. Returns a faked success after 600–1200ms (randomized). | Added missing article "a" before "faked success." |
| Form is fully keyboard-navigable. ↵ advances to next field if current is valid; ↑ goes back. Esc clears. Errors render as failing tests (`✗ email format — looks malformed`). | The form is fully keyboard-navigable. ↵ advances to the next field if the current field is valid; ↑ goes back. Esc clears. Errors render as failing tests (`✗ email format — looks malformed`). | Added article "The"; added articles before "next field" and "current"; supplied the elided noun "field" after "current" — bare "current" reads ambiguously as adjective-without-noun. |
| XP increments on the following actions, persisted in `localStorage["hm_xp_v1"]`: | XP increments on the following actions and is persisted in `localStorage["hm_xp_v1"]`: | Replaced the comma + participle (which dangles — "actions, persisted" reads as if the actions are persisted) with an explicit coordinated verb "and is persisted" so the subject of "persisted" is unambiguously XP. |
| XP capped at **100**. Increments idempotent per-action-per-session (visiting Network 5× still only grants +10 the first time). | XP is capped at **100**. Increments are idempotent per-action-per-session (visiting Network 5× still grants +10 only the first time). | Supplied verbs "is" and "are" in elliptical clauses; moved "only" adjacent to "the first time" to fix the squinting modifier (it was attaching to "grants" and reading as "still only grants" rather than "grants only the first time"). |
| Cross-component bus is `window.dispatchEvent(new CustomEvent("hm:xp", { detail: { delta, reason } }))`. No state-management library. | The cross-component bus is `window.dispatchEvent(new CustomEvent("hm:xp", { detail: { delta, reason } }))`. No state-management library is used. | Added article "The"; supplied a verb for the bare-fragment second sentence. |
| Keydown sequence buffer detects `↑↑↓↓←→←→BA` (case-insensitive on letters). 2-second timeout between keys resets the buffer. | The keydown-sequence buffer detects `↑↑↓↓←→←→BA` (case-insensitive on letters). A 2-second timeout between keys resets the buffer. | Added article "The"; hyphenated "keydown-sequence" as compound modifier; added article "A" before "2-second timeout"; this fixes both sentences starting with bare nouns. |
| Recruiter-mode footer includes an explicit "🎮 Show experimental" button for keyboard/discovery parity — clicking it sets the same unlock. | The Recruiter Mode footer includes an explicit "🎮 Show experimental" button for keyboard and discovery parity — clicking it sets the same unlock. | Capitalized "Recruiter Mode" to match the defined glossary term (used capitalized everywhere else in the PRD); added article; replaced "keyboard/discovery" slash with "and" — these are co-required parities, not alternatives. |
| `cmdk` palette opens on `⌘K` (macOS) / `Ctrl+K` (others). Also opens from the "Inspect me" CTA (FR-010) and the chrome's keyboard-shortcut hint. | The `cmdk` palette opens on `⌘K` (macOS) or `Ctrl+K` (others). It also opens from the "Inspect me" CTA (FR-010) and from the chrome's keyboard-shortcut hint. | Added article; replaced slash with "or"; supplied subject "It" for the second sentence (was a dangling continuation); repeated "from" for parallel prepositional structure. |
| Fuzzy search across all groups simultaneously. Result order: exact match > prefix > fuzzy. Keyboard ↑/↓/↵ navigation. Esc closes. | Fuzzy search runs across all groups simultaneously. Result order: exact match > prefix > fuzzy. Keyboard ↑/↓/↵ navigation is supported. Esc closes. | Supplied verbs for two bare-noun-phrase sentences (the staccato fragment style is inconsistent with FR-090 immediately above). |
| Palette respects `prefers-reduced-motion` — no scale/blur animation, just opacity. | The palette respects `prefers-reduced-motion` — no scale or blur animation, just opacity. | Added article; replaced "scale/blur" slash with "or" — these are both disabled together, but slash reads ambiguously. |
| Toggle exposed in **two** places: a chrome button (right of identity, `≥sm`) AND a ⌘K palette action under **Actions**. | The toggle is exposed in **two** places: a chrome button (right of identity, `≥sm`) AND a ⌘K palette action under **Actions**. | Added article and verb to the bare fragment opening. |
| When ON, navigation redirects to `/recruiter` and persists. Chrome (FR-001) is unmounted entirely (not just hidden via CSS). XP bar, Konami, REPL hints, palette XP-grant — all unmounted. | When ON, navigation redirects to `/recruiter` and the state persists. The chrome (FR-001) is unmounted entirely (not just hidden via CSS). The XP bar, Konami listener, REPL hints, and palette XP-grant logic are all unmounted. | Disambiguated the implicit subject of "persists" (navigation or state? — the latter is meant); added "The" before "chrome" for chrome-vs-Chrome disambiguation; in the final list, added Oxford "and" and clarified "Konami" (the listener), "palette XP-grant" (the logic) — bare nouns suggest a UI element rather than the underlying behavior. |
| Recruiter Mode is the only route that allows-but-doesn't-require dark/light. | Recruiter Mode is the only route that allows but does not require either dark or light mode. | Unhyphenated and expanded the compound verb-phrase (which doesn't parse cleanly); replaced "dark/light" slash with explicit "either dark or light mode." |
| Toggling OFF returns the user to `/` with full chrome restored. | Toggling OFF returns the user to `/` with the full chrome restored. | Added article "the." |
| `next-themes` provider mounted at root with `attribute="class"`, `defaultTheme="dark"`, `enableSystem={false}` (per resolved dark-only decision), `disableTransitionOnChange`. | The `next-themes` provider is mounted at root with `attribute="class"`, `defaultTheme="dark"`, `enableSystem={false}` (per the resolved dark-only decision), and `disableTransitionOnChange`. | Added article and verb to the bare-fragment opening; added "the" before "resolved dark-only decision"; added Oxford "and" before the final prop. |
| `D` key (no modifiers) toggles theme — currently a no-op since site is dark-only. | The `D` key (no modifiers) toggles theme — currently a no-op since the site is dark-only. | Added articles before "D key" and "site." |
| Lighthouse ≥95 across all four categories (Performance, Accessibility, Best Practices, SEO) on the deployed Vercel production URL, measured on mobile and desktop emulations. Real budget — CI fails if any category drops below 95. | Lighthouse ≥95 across all four categories (Performance, Accessibility, Best Practices, SEO) on the deployed Vercel production URL, measured on mobile and desktop emulations. This is a real budget — CI fails if any category drops below 95. | Replaced the noun-phrase fragment "Real budget" with a complete clause so the demonstrative reference is unambiguous; "Real budget" alone reads as a heading. |
| Interaction response <100ms (input → visible change). Animations sustain 60fps; jank budgeted only on the cold-cache first load. | Interaction response <100ms (input → visible change). Animations sustain 60fps; jank is budgeted only on the cold-cache first load. | Supplied missing verb "is" — without it, "jank budgeted" reads as a compound noun. |
| No external `<script>` tags. No external `<link rel="stylesheet">`. Fonts via `next/font/google` only. | No external `<script>` tags are allowed. No external `<link rel="stylesheet">` tags are allowed. Fonts are loaded via `next/font/google` only. | Supplied verbs for three bare-fragment sentences; the constraint is normative and "are allowed / are loaded" makes the modality explicit. |
| Adding any dependency >50KB gzipped requires explicit approval. No Three.js, no Lottie, no analytics SDKs beyond Vercel's built-in. | Adding any dependency >50KB gzipped requires explicit approval. No Three.js, no Lottie, and no analytics SDKs beyond Vercel's built-in are permitted. | Added Oxford "and"; supplied verb "are permitted" to complete the prohibition (matches NFR-P3 form). |
| Animation: `transform` and `opacity` only. Never `top`/`left`/`width`/`height`. Scroll reveals use `useInView({ once: true })`. | Animation: `transform` and `opacity` only. Never `top`, `left`, `width`, or `height`. Scroll reveals use `useInView({ once: true })`. | Replaced the slash-separated list of forbidden CSS properties with a comma-separated list (the items are equally forbidden, not alternatives among themselves). |
| Code-split heavy routes (`/console`, `/work/[slug]`, `/sources`) via `dynamic(() => import(...))`. | Code-split the heavy routes (`/console`, `/work/[slug]`, `/sources`) via `dynamic(() => import(...))`. | Added article "the"; the imperative verb is fine for an NFR instruction. |
| WCAG 2.1 AA compliance across all routes. Lime-on-Obsidian only used for large text (≥18pt, or ≥14pt bold) or icons — never body copy. | WCAG 2.1 AA compliance is required across all routes. Lime-on-Obsidian is used only for large text (≥18pt, or ≥14pt bold) or icons — never body copy. | Supplied "is required" to complete the first NFR fragment; moved "only" adjacent to "for large text" to fix the misplaced modifier ("only used for" reads as "used for nothing else"; intent is "used only for large text"). |
| Full keyboard navigation. Every interactive element reachable via Tab. Focus rings (`focus-visible:ring-1 focus-visible:ring-ring`) visible. | Full keyboard navigation is required. Every interactive element is reachable via Tab. Focus rings (`focus-visible:ring-1 focus-visible:ring-ring`) are visible. | Supplied verbs for three bare-fragment normative sentences. |
| `prefers-reduced-motion: reduce` gates every animation. No exceptions. XP bar fill still updates (instant); toasts hidden; tab transitions instant. | `prefers-reduced-motion: reduce` gates every animation. No exceptions. The XP bar fill still updates (instantly); toasts are hidden; tab transitions are instant. | Added article "The"; corrected adverb form "instant" → "instantly" where it modifies "updates"; supplied verb "are" in the last two clauses. |
| Semantic HTML — one `<h1>` per route, real `<nav>`, `<article>` for case studies, `<button>` not `<div onClick>`. | Use semantic HTML — one `<h1>` per route, real `<nav>`, `<article>` for case studies, and `<button>` instead of `<div onClick>`. | Added an imperative verb to the bare opening; added Oxford "and"; replaced the cryptic "not" with the clearer "instead of." |
| `alt` on every `<img>` (empty for decorative). `next/image` always used; never `<img>`. | Set `alt` on every `<img>` (empty for decorative images). Always use `next/image`; never use bare `<img>`. | Added imperative verbs to the two bare-fragment normative sentences; clarified "decorative" by adding the noun "images"; added "bare" before the final `<img>` for emphasis. |
| Breakpoints: mobile-first. Critical thresholds at `sm` (640px), `md` (768px), `lg` (1024px). All routes usable below 360px width. | Breakpoints follow a mobile-first strategy. Critical thresholds are at `sm` (640px), `md` (768px), and `lg` (1024px). All routes must remain usable below 360px width. | Supplied verbs for three bare-fragment NFR sentences; added Oxford "and"; "must remain usable" makes the requirement modality explicit. |
| Network waterfall has a dedicated mobile card layout (FR-020); desktop grid does not auto-degrade. | The Network waterfall has a dedicated mobile card layout (FR-020); the desktop grid does not auto-degrade. | Added two definite articles. |
| REPL stays usable on mobile (touch keyboard appears on focus; ↑/↓ history accessible via on-screen buttons under the input on mobile only). | The REPL remains usable on mobile (the touch keyboard appears on focus; ↑/↓ history is accessible via on-screen buttons under the input, on mobile only). | Added article; replaced colloquial "stays" with the more precise "remains"; added comma before the trailing scope qualifier "on mobile only" so it does not look like it modifies "input." |
| Every route has `<title>` (≤60 chars), meta `description` (≤160 chars), canonical URL, OG image. | Every route has a `<title>` (≤60 chars), a meta `description` (≤160 chars), a canonical URL, and an OG image. | Added articles to make the enumerated items grammatical noun phrases; added Oxford "and." |
| JSON-LD `Person` schema on `/`. `BreadcrumbList` on `/work/[slug]`. `WebSite` schema on `/`. | A JSON-LD `Person` schema on `/`. A `BreadcrumbList` schema on `/work/[slug]`. A `WebSite` schema on `/`. | Added articles for parallelism across the three SEO entries; added the noun "schema" to the middle entry so all three are grammatically parallel. |
| `robots.txt` + `sitemap.xml` generated by Next.js metadata APIs. | `robots.txt` and `sitemap.xml` are generated by the Next.js metadata APIs. | Replaced the technical "+" with "and"; supplied missing verb "are"; added article "the." |
| Per-case-study OG images generated via Next.js `opengraph-image.tsx` (dynamic). | Per-case-study OG images are generated via the Next.js `opengraph-image.tsx` route handler (dynamic). | Supplied missing verb "are"; named what `opengraph-image.tsx` is (a route handler) for explicit reference. (Optional — the bare filename may be intentionally compact.) |
| No client-side secrets. Contact form is stubbed in v1 (no env vars). v1.1 wires Resend via Next.js server action with `RESEND_API_KEY` (server-only). | No client-side secrets are used. The contact form is stubbed in v1 (no env vars). v1.1 will wire Resend via a Next.js server action with `RESEND_API_KEY` (server-only). | Supplied verb "are used" for first fragment; added article "The"; the future-tense "will wire" is more accurate than the present-tense "wires" since v1.1 is unbuilt; added article "a" before "Next.js server action." |
| Zod validates the contact form on submit — never trust HTML5 validation alone. | Zod validates the contact form on submit; never trust HTML5 validation alone. | Replaced the em dash with a semicolon: the two clauses are independent and the em dash here reads as an aside rather than as the imperative warning intended. |
| `dangerouslySetInnerHTML` forbidden except for owned/sanitized MDX (none in v1). | `dangerouslySetInnerHTML` is forbidden except for owned and sanitized MDX (none in v1). | Supplied missing verb "is"; replaced "owned/sanitized" slash with "and" — both properties must hold (slash is ambiguous as and/or). |
| CSP headers via `next.config.mjs` `headers()` when going live. | CSP headers are configured via `next.config.mjs` `headers()` when going live. | Supplied verb "are configured." |
| Deploy target: **Vercel** (zero-config). Branch previews automatic. CI relies on Vercel's build. | Deploy target: **Vercel** (zero-config). Branch previews are automatic. CI relies on Vercel's build. | Supplied missing verb "are" — bare "Branch previews automatic" reads as a fragment. |
| Yarn is the package manager. `yarn.lock` authoritative. Never `npm install`. | Yarn is the package manager. `yarn.lock` is authoritative. Never use `npm install`. | Supplied verb "is"; added verb "use" to the bare-imperative prohibition for grammaticality. |
| Pre-commit gate: `yarn typecheck && yarn lint && yarn test:run && yarn format`. Hook configured via `husky` or `simple-git-hooks`. | Pre-commit gate: `yarn typecheck && yarn lint && yarn test:run && yarn format`. The hook is configured via `husky` or `simple-git-hooks`. | Added article "The"; supplied verb "is." |
| Persistence: `localStorage` only, versioned keys (`hm_xp_v1`, `hm_unlocks_v1`, `hm_visits_v1`, `hm_recruiter_v1`). Graceful degradation if `localStorage` unavailable (in-memory state, no crash). | Persistence: `localStorage` only, with versioned keys (`hm_xp_v1`, `hm_unlocks_v1`, `hm_visits_v1`, `hm_recruiter_v1`). Graceful degradation applies if `localStorage` is unavailable (in-memory state, no crash). | Added "with" to make the appositive grammatical; supplied verbs "applies" and "is." |
| UI primitives via **shadcn 4.8.0** (`radix-nova` style preset) on **Tailwind v4.2.1**. No alternative component library; shadcn primitives are vendored into `components/ui/*` and reused before any new component is hand-rolled. Replacing or adding a parallel primitive system requires explicit approval. | UI primitives come via **shadcn 4.8.0** (`radix-nova` style preset) on **Tailwind v4.2.1**. No alternative component library is permitted; shadcn primitives are vendored into `components/ui/*` and reused before any new component is hand-rolled. Replacing or adding a parallel primitive system requires explicit approval. | Supplied verb "come"; supplied verb "is permitted" for the prohibition clause. |
| 4 companies of work experience (Buguard, MasheedGate, Inovola, Besteam) with 8 nested projects. | 4 companies of work experience (Buguard, MasheedGate, Inovola, and Besteam) with 8 nested projects. | Added Oxford "and" — applies consistently to other parenthetical lists in §7.1. |
| 3 freelance companies (PickPath/Commutrics, Grand Community, Alsakn) with 4 projects. | 3 freelance companies (PickPath / Commutrics, Grand Community, and Alsakn) with 4 projects. | Added Oxford "and"; added spaces around the slash in "PickPath / Commutrics" so it does not visually merge with the surrounding tokens. (Slash kept intentionally — it indicates an alternate name for one entity, not a list separator.) |
| 22 projects in the flat gallery (overlaps with company-nested projects — needs dedup). | 22 projects in the flat gallery (overlaps with company-nested projects — these need deduplication). | Supplied explicit subject "these" — "needs dedup" is a fragment with no clear antecedent (singular "needs" disagrees with plural "projects"); expanded slang "dedup" to "deduplication." |
| 28 skills across 3 tiers (Main, Basics, Tools). | 28 skills across 3 tiers (Main, Basics, and Tools). | Added Oxford "and." |
| **Profile identity:** name, location, email, socials, tagline, principles[], metrics[]. | **Profile identity:** name, location, email, socials, tagline, `principles[]`, and `metrics[]`. | Backticked the array-typed fields (`principles[]`, `metrics[]`) for consistency with elsewhere in the PRD where typed identifiers are inline-code; added Oxford "and." |
| All 22 projects (deduplicated) appear in the `/work` waterfall. | All 22 projects (deduplicated) appear in the `/work` waterfall. (Consider: confirm whether the dedup target count is exactly 22 — §7.1 says "22 projects… needs dedup," implying the count may drop after dedup.) | Query: there is a possible internal inconsistency between §7.1 (22 projects need dedup) and §7.3 (all 22 dedup'd projects appear). If dedup reduces the count, "All 22" is wrong. Flagged. |
| **6 projects** get full case studies. **Default picks (revised after content-fidelity audit):** Dark Atlas, MasheedGate E-commerce, Tamincom Refactor, Zrealtors, Commutrics, Eazy.to. | **6 projects** get full case studies. **Default picks (revised after the content-fidelity audit):** Dark Atlas, MasheedGate E-commerce, Tamincom Refactor, Zrealtors, Commutrics, and Eazy.to. | Added article "the"; added Oxford "and" before the final list item. |
| Rationale: these 6 have the most pre-authored role-bullet content in legacy `lib/data/index.ts`, minimizing case-study mock surface. | Rationale: these 6 have the most pre-authored role-bullet content in the legacy `lib/data/index.ts`, which minimizes case-study mock surface. | Added article "the"; replaced the dangling participial phrase ("minimizing…" had no clear subject) with an explicit relative clause whose antecedent is unambiguous. |
| Buguard Dashboards and BuilderZ are demoted to waterfall-only because their legacy descriptions are duplicated from sibling projects (Buguard ≈ Dark Atlas text, BuilderZ thin). | Buguard Dashboards and BuilderZ are demoted to waterfall-only because their legacy descriptions are duplicated from sibling projects (Buguard ≈ Dark Atlas text; BuilderZ description is thin). | Replaced the comma between the two parenthetical clauses with a semicolon (they are independent observations); supplied verb "description is" — "BuilderZ thin" reads as nonsense without it. |
| Hossam can swap any pick at start of Phase 4. | Hossam can swap any pick at the start of Phase 4. | Added article "the." |
| Remaining 16 projects show in waterfall with a "View source" link only. | The remaining 16 projects appear in the waterfall with a "View source" link only. | Added articles; replaced "show in" (intransitive use of a transitive verb — colloquial) with the more precise "appear in." |
| Skills matrix in Recruiter Mode shows all 28 in 3 columns. | The skills matrix in Recruiter Mode shows all 28 skills in 3 columns. | Added article; supplied the elided noun "skills" — bare "all 28" relies on the reader carrying forward the antecedent from the previous paragraph. |
| Wherever real content is missing, the typed Project entry includes a `[MOCK]` flag in a `meta.mock` boolean field. Mocks are colored visually identical to real entries but logged to the console on render: `console.warn("[devtools://hossam] Mock content rendered for slug: <slug>")`. Pre-launch gate: zero mocks remain (CI check greps for `mock: true`). | Wherever real content is missing, the typed Project entry includes a `[MOCK]` flag in a `meta.mock` boolean field. Mocks are visually identical to real entries but are logged to the console on render: `console.warn("[devtools://hossam] Mock content rendered for slug: <slug>")`. Pre-launch gate: zero mocks remain (the CI check greps for `mock: true`). | Removed "colored" (redundant or incorrect — "colored visually identical" is awkward); supplied verb "are" before "logged" to make the parallel structure grammatical; added article "the" before "CI check." |
| `lib/data/index.ts` → `lib/content/projects.ts`. See addendum §1 for the field-mapping table. | Migrate `lib/data/index.ts` → `lib/content/projects.ts`. See addendum §1 for the field-mapping table. | Supplied an imperative verb — the bare arrow notation as a section opener is parseable but ambiguous about who acts. |
| 3-week sprint, evenings/weekends. | A 3-week sprint, working evenings and weekends. | Added article and present-participle phrase so the section header is a proper noun phrase (the sprint structure is the subject); replaced slash with "and" — work happens on both, not one or the other. |
| Vercel Analytics provides: page-view counts per route, top referrers, country, device class. It does **not** provide: per-session duration, multi-page funnel completion, custom event tracking. | Vercel Analytics provides page-view counts per route, top referrers, country, and device class. It does **not** provide per-session duration, multi-page funnel completion, or custom event tracking. | Removed the unnecessary colons after "provides" (the lists are direct objects, not appositives); added Oxford conjunctions to both lists. |
| Metrics below are marked `[Vercel]` (computable from Vercel alone) or `[v1.1]` (require Plausible/PostHog, deferred). | The metrics below are marked `[Vercel]` (computable from Vercel alone) or `[v1.1]` (requires Plausible or PostHog; deferred). | Added article "The"; fixed verb agreement ("require" → "requires" — the subject is the singular metric, not the tools); replaced "Plausible/PostHog" slash with "or"; replaced internal comma with semicolon to mark the deferred status as a separate property. |
| A v1.1 follow-up adds Plausible (≤2KB self-hosted-style or hosted) if the deferred metrics become important. | A v1.1 follow-up will add Plausible (≤2KB, self-hosted-style or hosted) if the deferred metrics become important. | Future-tense "will add" reflects that v1.1 is unbuilt; added a comma so the size constraint is clearly separate from the hosting-style descriptor. |
| Average session duration ≥ 90 seconds (excludes recruiter-mode bounces). Below 30s = the hook is failing. *v1 proxy: track manually by sharing the URL with 5 trusted reviewers and asking "did you read past the hero?"* | Average session duration ≥ 90 seconds (excludes Recruiter Mode bounces). Below 30s means the hook is failing. *v1 proxy: track manually by sharing the URL with 5 trusted reviewers and asking "Did you read past the hero?"* | Capitalized "Recruiter Mode" to match the glossary term (used capitalized in §3, F12, etc.); replaced the technical "=" with the prose connector "means" (in a body-text metric definition); capitalized the first word of the quoted question. |
| Case-study view-through rate ≥ 30% (visitors who land on `/` reach at least one `/work/[slug]`). | Case-study view-through rate ≥ 30% (visitors who land on `/` and reach at least one `/work/[slug]`). | Added "and" — without it, the parenthetical reads as a single declarative ("visitors land and reach"), which conflicts with the metric being a *rate of* such visitors. The intended meaning is the proportion who do both. |
| Recruiter Mode toggle rate ≤ 15% of total sessions. Too high = chrome is alienating P1. | Recruiter Mode toggle rate ≤ 15% of total sessions. A higher rate means the chrome is alienating P1. | Replaced bare "Too high" fragment + "=" with a complete sentence using "means"; added article "the" before "chrome." |
| Lighthouse score on the live URL stays ≥95 across all categories for 4 weeks straight. | The Lighthouse score on the live URL stays ≥95 across all categories for 4 consecutive weeks. | Added article "The"; replaced colloquial "4 weeks straight" with the more precise "4 consecutive weeks." |
| Interview rate from outreach using devtools://hossam as the share link is ≥2× the rate from LinkedIn-only outreach. | The interview rate from outreach using devtools://hossam as the share link is ≥2× the rate from LinkedIn-only outreach. | Added article "The." |
| At least one interviewer mentions the portfolio unprompted ("I liked the X"). | At least one interviewer mentions the portfolio unprompted (e.g., "I liked the [X]"). | Clarified the placeholder — bare quoted "X" is ambiguous as to whether it is a literal letter or a placeholder; added "e.g." to mark the quoted string as an example. |
| Bounce rate >75% on `/work` = waterfall is too clever. Triage by adding a clearer "How to read this" affordance. | A bounce rate >75% on `/work` means the waterfall is too clever. Triage by adding a clearer "How to read this" affordance. | Added article; replaced "=" with prose connector "means"; added article "the" before "waterfall." |
| Recruiter Mode toggle rate >40% = chrome is actively repelling P1. | A Recruiter Mode toggle rate >40% means the chrome is actively repelling P1. | Same change as the previous row — applied consistently to the CM2 phrasing. |
| Console route page views <2% of total = it's invisible. Triage by adding a discovery hint in the ⌘K palette description. | Console route page views <2% of total means the route is invisible. Triage by adding a discovery hint in the ⌘K palette description. | Replaced "=" with "means"; replaced the unclear antecedent "it's" with the explicit "the route is" (the previous-clause subject could be "Console route page views" or "Console route" — disambiguated). |
| **Decision rule for adding analytics in v1.1:** if any `[v1.1]` proxy gives ambiguous signal *and* a real product decision hinges on the answer, ship Plausible. Otherwise stay on Vercel-only. | **Decision rule for adding analytics in v1.1:** if any `[v1.1]` proxy gives an ambiguous signal *and* a real product decision hinges on the answer, ship Plausible. Otherwise, stay on Vercel-only. | Added article "an" before "ambiguous signal"; added comma after sentence-initial "Otherwise." |
| ❌ Real contact backend (Resend, etc.) — stubbed in v1, v1.1 follow-up. | ❌ Real contact backend (Resend, etc.) — stubbed in v1; deferred to a v1.1 follow-up. | Replaced the comma splice with a semicolon and a verb ("deferred to") — the bare "v1.1 follow-up" was a noun-phrase fragment of unclear relationship to the preceding clause. |
| ❌ CMS / MDX content pipeline — content in typed TS files only. | ❌ CMS or MDX content pipeline — content lives in typed TS files only. | Replaced slash with "or"; supplied verb "lives" — bare "content in" is fragmentary. |
| ❌ Multi-language (EN/AR) — fonts already loaded but no language toggle UI in v1. | ❌ Multi-language (EN / AR) — fonts are already loaded but no language-toggle UI ships in v1. | Spaced the slash in "EN / AR" so it does not visually collide with the surrounding text; supplied verb "are"; hyphenated "language-toggle" as compound modifier; supplied verb "ships." |
| ❌ Analytics beyond Vercel built-in. No GA, no PostHog in v1. | ❌ Analytics beyond Vercel built-in. No GA or PostHog in v1. | Replaced repeated "no" with single "no… or" for tighter parallel; the original is acceptable but the revision is more concise. (Optional.) |
| ❌ Blog / articles section — file-tree placeholder only. | ❌ Blog or articles section — file-tree placeholder only. | Replaced slash with "or." |
| ❌ Talks / conference page — file-tree placeholder only. | ❌ Talks or conference page — file-tree placeholder only. | Replaced slash with "or." |
| ❌ Live Lighthouse score in `/perf` (FR-052) — deferred to v1.1. | _(no change)_ | This entry is well-formed. |
| ❌ A11y testing automation (axe in CI) — manual audit for v1; automate later. | ❌ A11y testing automation (axe in CI) — manual audit in v1; automation deferred to v1.1. | Replaced "for v1" with "in v1" for parallel construction with neighboring out-of-scope entries; replaced bare "automate later" imperative with explicit "automation deferred to v1.1" matching the rest of the section's deferral phrasing. |
| ❌ Custom 404 / 500 with DevTools flavor — basic Next.js default in v1, custom in v1.1. | ❌ Custom 404 / 500 with DevTools flavor — basic Next.js defaults ship in v1; custom shells ship in v1.1. | Pluralized "default" → "defaults" (one each for 404 and 500); supplied verb "ship"; replaced comma splice with semicolon; supplied the missing noun "shells" (matches §5.0 voice rule wording). |
| ❌ PWA / offline-first — explicitly dropped with Dexie. | ❌ PWA or offline-first — explicitly dropped with Dexie. | Replaced slash with "or." |
| ❌ Custom-domain DNS configuration — `hossammarey.com` (or chosen domain) is a deploy-day task, not feature work. | ❌ Custom-domain DNS configuration — `hossammarey.com` (or the chosen domain) is a deploy-day task, not feature work. | Added article "the." |
| These are deferrable — the build phases above proceed with stated assumptions. Each OQ resolution is a small content/scope tweak, not an architectural one. | These are deferrable — the build phases above proceed with the stated assumptions. Each OQ resolution is a small content or scope tweak, not an architectural one. | Added article "the"; replaced "content/scope" slash with "or" — these are alternative tweak kinds, not a single compound. |
| **Status legend:** *Confirmed* = decided by Hossam or by external evidence; *Pending* = needs explicit confirmation before downstream work depends on it. | **Status legend:** *Confirmed* — decided by Hossam or by external evidence; *Pending* — needs explicit confirmation before downstream work depends on it. | Replaced the "=" with em dashes for prose-register consistency (the "=" is appropriate inside the table cells but reads as code outside them). |
| Full log: `.decision-log.md` in this folder. | The full log lives in `.decision-log.md` in this folder. | Added article and verb to the bare-fragment parenthetical for grammaticality. |
| Dark-only palette (Obsidian + Signal Lime), per `docs/design-system.md`. | Dark-only palette (Obsidian and Signal Lime), per `docs/design-system.md`. | Replaced the technical "+" with "and" in body-text context (the "+" reads as concatenation operator). |
| `localStorage`-only persistence; Dexie dropped 2026-05-25. | `localStorage`-only persistence; Dexie was dropped on 2026-05-25. | Supplied verb "was" and preposition "on" — the bare "Dexie dropped 2026-05-25" reads as a fragment. |
| Contact stub v1, Resend v1.1. | Contact form stubbed in v1; Resend integration ships in v1.1. | Supplied verbs and the elided noun "form" / "integration"; replaced comma splice with semicolon. |
| Recruiter Mode dual-toggle (chrome + ⌘K). | Recruiter Mode has a dual toggle (chrome and ⌘K). | Supplied verb "has"; replaced "+" with "and"; un-hyphenated "dual-toggle" (the term is a noun phrase here, not a compound modifier). |
| Vercel deployment, zero-config. | Vercel deployment is zero-config. | Supplied verb "is" — the bare appositive reads as a fragment in a list of full-sentence bullets. |
| `_bmad-output/project-context.md` — 142-rule AI-agent ruleset (2026-05-25). | `_bmad-output/project-context.md` — the 142-rule AI-agent ruleset (last updated 2026-05-25). | Added article "the"; clarified the bare date as "(last updated 2026-05-25)" so the reader knows what the date refers to. |
| `docs/plan.md` — original IA + features doc (pending Next.js rewrite). | `docs/plan.md` — the original IA and features doc (pending Next.js rewrite). | Added article; replaced "+" with "and" in body-text context. |
| `docs/tech-equirements.md` — DEPRECATED (template-legacy NFRs). | `docs/tech-requirements.md` — DEPRECATED (template-legacy NFRs). | Typo fix: "tech-equirements" → "tech-requirements" (missing "r"). Verify the file actually exists at the corrected path; if the deprecated file genuinely has the misspelled name on disk, restore. |
| `_bmad-output/planning-artifacts/prds/prd-web-2026-05-25/addendum.md` — content migration mapping + technical depth that didn't fit the PRD body. | `_bmad-output/planning-artifacts/prds/prd-web-2026-05-25/addendum.md` — content-migration mapping and technical depth that did not fit in the PRD body. | Hyphenated "content-migration" as compound modifier; replaced "+" with "and"; expanded contraction "didn't" to "did not" for the LLM-targeted formal register; added "in" for prepositional clarity. |

## Cross-cutting issues (applied across multiple locations)

| Pattern | Locations | Fix | Reason |
|---|---|---|---|
| Slash `/` used as a disjunction in body prose | UJ-2 ("ongoing/archived noise"; "Twitter/HN"; "LinkedIn / Slack"); FR-021 ("rewrites/refactors", "small / contract"); FR-022 ("`--status-ok` / `--status-warn` / `--status-err`"); FR-023 ("impact/scope"); FR-031 ("before/after"); FR-034 ("source repo / live URL"); FR-042 ("theme dark / theme light"); FR-050 ("talks/articles"; "mentees/talks"); FR-051 ("HTML / JS / CSS / images / fonts"); FR-074 (cross-link); FR-083 ("keyboard/discovery"); FR-090 ("⌘K / Ctrl+K"); FR-093 ("scale/blur"); FR-103 ("dark/light"); NFR-P5 ("`top`/`left`/`width`/`height`"); §7.1 (`PickPath/Commutrics`); §7.3 ("content/scope"); §10 multiple ("CMS / MDX", "EN/AR", "PWA / offline-first", "Blog / articles", "Talks / conference"); App. B ("IA + features") | Replace each slash with "or" / "and" / a comma list, as appropriate to the logical relation. | For an LLM reader the slash is ambiguous between conjunction and disjunction; the Microsoft Writing Style Guide prefers explicit "and" / "or" / list form in body prose. (Filenames, URLs, and code identifiers retain the slash.) |
| Bare-fragment NFR sentences (no verb) | NFR-P1 ("Real budget"), NFR-P3 (three fragments), NFR-P4 ("No Three.js…"), NFR-A1 ("Lime-on-Obsidian only used…"), NFR-A2 (three fragments), NFR-A4 ("Semantic HTML…"), NFR-A5 (two fragments), NFR-R1 (three fragments), NFR-S3, NFR-S4, NFR-SE1, NFR-SE3, NFR-SE4, NFR-O1, NFR-O2, NFR-O3, NFR-O4, NFR-O5 | Supply explicit verbs and articles. | For NFRs that are normative requirements ("X must Y"), explicit verbs make the modality unambiguous; LLM agents parsing the requirements need the predicate to extract the constraint. |
| Bare "Chrome" without article | FR-001, FR-101, several §5.0 references | Add definite article "the chrome" to avoid Chrome (the browser) vs. chrome (the UI frame) ambiguity. | Glossary §12 itself warns of this confusion. |
| "Recruiter Mode" inconsistently cased | FR-083 ("Recruiter-mode footer"), M1 ("excludes recruiter-mode bounces") | Capitalize as "Recruiter Mode" everywhere (it is a defined term per Glossary §12). | Consistent rendering of glossary terms aids both human reviewers and LLM downstream agents. |
| "=" used as a prose connector outside tables | M1 proxy lines, CM1, CM2, CM3, Status legend | Replace with "means" / em-dash. | "=" reads as a code/math operator; in body prose, "means" or em-dash conveys the connection without ambiguity. |
| "+" used as a prose connector | App. A ("Obsidian + Signal Lime"); App. B ("IA + features"); FR-083 (in "keyboard/discovery parity" structure) | Replace with "and." | Same reasoning as above. |
| Telegraphic list with no Oxford "and" | §3 P1 "Wants:" and "Does not want:" lists; §3 P2 lists; §3 P3 "Wants:"; FR-060 file-tree entries; §7.1 enumerations; §7.3 picks list | Add the Oxford "and" before the final list item. | Microsoft Writing Style Guide recommends serial comma + "and" for unambiguous enumeration. |

## Summary

- **Total fixes proposed:** 92 row-level edits + 7 cross-cutting pattern rollups (capturing dozens of additional occurrences not enumerated individually).
- **Top categories:**
  - **Slash-as-disjunction in prose** (≈22 occurrences) — replace with "or" / "and" / comma list.
  - **Bare-fragment sentences missing verb or article** (≈30+ occurrences, concentrated in NFRs and FR bullets).
  - **Article omission / definite-reference ambiguity** (≈18 occurrences).
  - **"Chrome" capitalization / disambiguation** (≈4 occurrences) — add article to avoid Chrome-browser collision.
  - **"=" / "+" as prose connectors** (≈8 occurrences) — replace with "means" / "and."
  - **Inconsistent capitalization of glossary terms** (Recruiter Mode lowercased twice).
  - **Telegraphic-list missing Oxford "and"** (≈10 occurrences).
  - **Squinting / misplaced modifiers** (NFR-A1 "only used for"; F9 "still only grants").
  - **Pronouns with ambiguous antecedents** (§5.0 "raise it"; CM3 "it's invisible"; §F9 "navigation… persists").
  - **Tense inconsistency** (v1.1 statements in present tense — should be future).
  - **Comma splices** (FR-034, App. A "Contact stub v1, Resend v1.1," §10 "stubbed in v1, v1.1 follow-up").
  - **One typo:** App. B `docs/tech-equirements.md` → `docs/tech-requirements.md`.
  - **One internal-consistency query:** §7.3 "All 22 projects (deduplicated)" vs. §7.1 "22 projects… needs dedup" — verify whether dedup changes the count.
  - **One gendered-pronoun replacement:** §1 "can he build" → "can this person build."
- **Sections intentionally untouched:**
  - §5.0 Voice & Tone — preserved as authorial voice per project style note.
  - All frontmatter, code blocks, table separator rows, and inline code identifiers.
