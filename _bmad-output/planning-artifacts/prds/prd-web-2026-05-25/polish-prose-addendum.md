# Editorial Review — Prose Polish: PRD Addendum

Reader type: `llm` (precision focus — unambiguous references, consistent terminology, explicit structure, no hedging).
Scope: prose only. Code blocks, frontmatter, and table separator rows skipped. Technical code content (Zod schemas, CSS, TypeScript) treated as authoritative and left untouched.

| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| Depth that earned its place but doesn't fit the PRD body. Consume alongside `prd.md`. Architecture, content sourcing, rejected alternatives, and migration mechanics live here. | Depth that earned its place but doesn't fit the PRD body. Read alongside `prd.md`. Architecture, content sourcing, rejected alternatives, and migration mechanics live here. | "Consume" is ambiguous as a directive to an LLM reader. "Read" is the unambiguous verb for the intended action. |
| The PRD references token names (`background`, `lime`, `hairline`, etc.) without inlining values. This section is the authoritative list. Copy these into `app/globals.css` under `@theme inline { ... }`. Values are OKLCH per `docs/design-system.md`. | The PRD references token names (`background`, `lime`, `hairline`, etc.) without inlining values. This section is the authoritative list. Copy these tokens into `app/globals.css` under `@theme inline { ... }`. Values are OKLCH per `docs/design-system.md`. | Replaced pronoun "these" with explicit noun phrase "these tokens" — LLM-precision: removes referential ambiguity (could otherwise read as referencing "values"). |
| These are not preferences. They are project identity. Violating them produces something that looks like a generic dashboard, not devtools://hossam. | These rules are not preferences. They are project identity. Violating them produces something that looks like a generic dashboard, not devtools://hossam. | Anchors the opening pronoun "These" to the explicit antecedent "rules" — the section heading is "Aesthetic non-negotiables," but pronouns at paragraph-open can confuse downstream LLM consumers. |
| **No drop shadows on cards / panels / modals.** Hairline borders only. Depth comes from background-color steps (`background` → `surface` → `surface-2`), never from `box-shadow`. | **No drop shadows on cards, panels, or modals.** Hairline borders only. Depth comes from background-color steps (`background` → `surface` → `surface-2`), never from `box-shadow`. | Replaced slash-separated list with comma-and-"or" form. Slashes between nouns are ambiguous (could read as "and/or," "versus," or "alias for"). Applies consistently in subsequent items too. |
| **Maximum border radius is `--radius` (6px).** Tailwind `rounded-sm`, `rounded`, occasionally `rounded-md`. Never `rounded-lg`, `rounded-xl`, or `rounded-full` except for the chrome XP bar pill. | **Maximum border radius is `--radius` (6px).** Use Tailwind `rounded-sm` and `rounded`, occasionally `rounded-md`. Never use `rounded-lg`, `rounded-xl`, or `rounded-full`, except for the chrome XP bar pill. | The original is a sentence fragment with no verb. Added "Use … Never use …" to make both clauses explicit and parallel. |
| The `bg-hairline` parent with `gap-px` between `bg-surface` children produces the inset-hairline grid effect of Chrome DevTools' Computed tab. Use this for: principles panel on `/`, case-study Decisions/Outcomes sections, sources file-tree right pane, REPL output blocks. | The `bg-hairline` parent with `gap-px` between `bg-surface` children produces the inset-hairline grid effect of Chrome DevTools' Computed tab. Use this idiom for: the principles panel on `/`, case-study Decisions and Outcomes sections, the sources file-tree right pane, and REPL output blocks. | Replaced bare "this" with "this idiom" (explicit antecedent); replaced slash "Decisions/Outcomes" with "Decisions and Outcomes" to remove slash ambiguity; added missing articles and a serial "and" so the list parses as a list rather than a run-on. |
| **Inputs sit on a background *darker* than `--surface`** (`oklch(0.13 0.012 260)` / `--input`), not lighter. Focus state is a 1px lime border (`focus:border-lime`), not a glow ring. | **Inputs sit on a background *darker* than `--surface`** (`oklch(0.13 0.012 260)`, exposed as `--input`), not lighter. Focus state is a 1px lime border (`focus:border-lime`), not a glow ring. | The slash between the OKLCH value and `--input` is ambiguous (alternative? equivalence? alias?). Spelled out the relationship explicitly. |
| **`::selection` is lime-on-obsidian-inverted** (lime background, dark foreground). Highlighting text should feel like the site is selecting *you*. | **`::selection` is lime-on-obsidian-inverted** (lime background, dark foreground): highlighting text should feel like the site is selecting *you*. | Joined the two sentences with a colon — the second is a gloss on the first, not an independent claim, and the colon signals that relationship to the LLM consumer. |
| **No gradient fills** anywhere except where the spec explicitly calls them out (the `.bg-grid` + `.bg-scan` hero, where they're rgba-grayscale not chromatic). | **No gradient fills** anywhere except where the spec explicitly calls them out (the `.bg-grid` and `.bg-scan` hero background, where they're rgba-grayscale, not chromatic). | Replaced "+" with "and" in prose (the "+" reads as CSS-class concatenation but is being used as English conjunction); added missing noun "background"; added comma before "not" for the contrast clause. |
| **Lime is a punctuation color, not a paint color.** Used for: primary CTAs, active tab underline, XP bar fill, focus rings, palette highlight, brand accent. **Never** used for body copy, large filled surfaces, or anything that turns the site into a Doritos ad. If a section reads "too green," it is too green. | **Lime is a punctuation color, not a paint color.** Use lime for: primary CTAs, the active tab underline, the XP bar fill, focus rings, the palette highlight, and the brand accent. **Never** use lime for body copy, large filled surfaces, or anything that turns the site into a Doritos ad. If a section reads "too green," it is too green. | Replaced sentence fragments ("Used for:", "Never used for") with explicit imperatives. Replaced implicit pronoun reference with the explicit noun "lime" in both clauses. Added serial "and" and articles for list readability. |
| **All animations gated by `prefers-reduced-motion`** — restated here because it's an aesthetic decision, not just an a11y one. The site should feel quiet, not busy. | **All animations gated by `prefers-reduced-motion`** — restated here because it is an aesthetic decision, not only an accessibility one. The site should feel quiet, not busy. | Expanded "it's" to "it is" (LLM-mode prefers unambiguous tokens over contractions in normative statements); replaced "just" with "only" (more precise); spelled out "a11y" → "accessibility" on first prose use in this addendum. |
| Experience and Skill schemas live in `lib/content/experience.ts` and `lib/content/skills.ts` respectively. Design-system.md §13 only explicitly types `Profile` and `Project`; Experience/Skill are inferred from `lib/data/index.ts` shape: | The Experience and Skill schemas live in `lib/content/experience.ts` and `lib/content/skills.ts` respectively. `docs/design-system.md` §13 explicitly types only `Profile` and `Project`; the Experience and Skill schemas are inferred from the `lib/data/index.ts` shape: | Added articles for grammaticality; restored the doc path prefix `docs/` (used everywhere else in the addendum) for consistency; moved "only" next to its scope ("only `Profile` and `Project`"); replaced "Experience/Skill" slash form with explicit conjunction. |
| Same project appears in both `experience[].projects[]` and the flat `projects[]`. Naive name-matching FAILS for at least 6 known cases — the legacy data has casing variance, parent-child splits, and one→many situations: | The same project appears in both `experience[].projects[]` and the flat `projects[]`. Naive name-matching FAILS for at least 6 known cases — the legacy data has casing variance, parent-child splits, and one-to-many situations: | Added the definite article "The" to anchor the subject. Replaced unicode arrow "→" in the prose phrase "one→many" with the standard English "one-to-many" (the arrow is reserved for the mapping notation elsewhere; mixing the two creates inconsistent prose conventions). |
| **The dedup is therefore not algorithmic. It is a hand-authored mapping table** committed alongside the migration script at `scripts/migrate-content/legacy-mapping.ts`: | **Dedup is therefore not algorithmic; it is a hand-authored mapping table,** committed alongside the migration script at `scripts/migrate-content/legacy-mapping.ts`: | Replaced sentence break with semicolon to keep the contrast tight (the second clause is a refinement of the first, not a new claim). Removed the leading "The" — "dedup" reads as a process noun and doesn't need the article. |
| **Why hand-authored:** the data is small (~30 legacy entries). The cost of getting dedup wrong is high (duplicated case studies, broken refs, missing role bullets). A name-similarity heuristic would silently produce wrong output. A mapping table makes every decision auditable. | **Why hand-authored:** the dataset is small (~30 legacy entries). The cost of getting dedup wrong is high (duplicated case studies, broken refs, missing role bullets). A name-similarity heuristic would silently produce wrong output. A mapping table makes every decision auditable. | "The data is" vs. "the dataset is": "dataset" is the more precise referent (the corpus, not the abstract notion), and matches usage in adjacent migration sections. |
| For any project missing case-study fields (problem/role/decisions/outcomes), set `meta.mock: true` and populate with lorem-ipsum-style placeholder. Render path emits `console.warn` in dev (FR §7.4). Pre-launch CI check: | For any project missing case-study fields (`problem`, `role`, `decisions`, `outcomes`), set `meta.mock: true` and populate with a lorem-ipsum-style placeholder. The render path emits `console.warn` in dev (FR §7.4). Pre-launch CI check: | Wrapped the field names in code formatting (they are field identifiers used elsewhere in backticks); replaced slash-separated list with comma-separated; added missing article "a" before "lorem-ipsum-style placeholder" and "The" before "render path." |
| (or programmatic — check `Project.meta.mock === false` for all entries in the v1 case-study list of 6.) | (Or programmatic: check that `Project.meta.mock === false` for all 6 entries in the v1 case-study list.) | Capitalized "Or" (sentence in parens stands on its own); replaced em-dash with colon (the second half is a definition, not an aside); added "that" after "check" for clarity; moved "6" next to "entries" for natural reading order. |
| Original TanStack notation → Next.js 16 App Router: | Mapping from the original TanStack notation to the Next.js 16 App Router: | Replaced the unicode arrow with explicit English. The arrow as a prose connector is ambiguous (especially to LLMs that may parse it as code/operator); the table that follows uses arrows for its mapping rows, so the prose lead-in should distinguish itself. |
| Recruiter Mode special handling: route is `/recruiter` but the chrome (rendered in `app/layout.tsx`) must conditionally skip when `pathname === "/recruiter"`. Pattern: | Recruiter Mode requires special handling: the route is `/recruiter`, but the chrome (rendered in `app/layout.tsx`) must skip rendering when `pathname === "/recruiter"`. Pattern: | The original was a noun-phrase header followed by a comma-stitched fragment. Made it a complete sentence with a verb ("requires"), added the missing article "the," and replaced the bare adverb "conditionally skip" with explicit "skip rendering" (the condition is already stated by the `when` clause). |
| Or alternatively: use a Route Group `(chrome)` for the 6 chrome'd routes and put `/recruiter` outside it. Cleaner. Recommended. | Alternatively, use a Route Group `(chrome)` for the 6 chromed routes and put `/recruiter` outside it. This is cleaner and is the recommended approach. | "Or alternatively" is redundant ("alternatively" already implies "or"); "chrome'd" is non-standard — "chromed" is the conventional verbal-adjective form; one-word verdicts ("Cleaner. Recommended.") are ambiguous to LLM readers — promoted to a full sentence stating the recommendation. |
| Considered for chrome-persistent tab swaps. Rejected because: | Considered for chrome-persistent tab swaps. Rejected for these reasons: | "Rejected because:" is a sentence fragment ending in "because" followed by a list — grammatically incomplete. "Rejected for these reasons:" is a complete sentence with an explicit referent for the list that follows. Applies in the same form to §§3.2, 3.3, 3.4, 3.5. |
| Browser support gaps for Firefox at time of PRD. | Browser support gaps in Firefox at the time of this PRD. | "for Firefox" reads as ambiguous (gaps *experienced by* Firefox vs. *targeting* Firefox); "in Firefox" is unambiguous. Added the missing article "the" and the demonstrative "this" to anchor the temporal reference. |
| `motion/react` already a dep; adding View Transitions API on top doubles the animation system. | `motion/react` is already a dependency; adding the View Transitions API on top doubles the animation system. | Restored the missing verb "is" (the original is a fragment); spelled out "dependency" (full word in prose; "dep" reads as code-jargon shorthand and is inconsistent with surrounding prose); added "the" before "View Transitions API." |
| AnimatePresence + layoutId gives equivalent visual quality with full reduced-motion control. | AnimatePresence with `layoutId` gives equivalent visual quality with full reduced-motion control. | Replaced "+" used as English conjunction with "with"; wrapped the identifier `layoutId` in backticks to match surrounding code-token formatting. |
| Considered for richer case-study content authoring (code blocks, embedded charts). Rejected because: | Considered for richer case-study content authoring (code blocks, embedded charts). Rejected for these reasons: | Same fragment fix as in §3.1. |
| MDX pipeline adds 2 deps (`@next/mdx`, `remark-*`) — ~80KB to bundle. | An MDX pipeline adds 2 dependencies (`@next/mdx`, `remark-*`) — roughly 80 KB to the bundle. | Added the indefinite article "An"; spelled out "dependencies"; replaced the tilde "~" inside prose with the explicit word "roughly" (tilde-as-prefix is ambiguous to LLM consumers — it can read as bitwise-NOT or as the home-directory marker); added the missing "the" before "bundle." |
| Case studies only need text + chips + bullets + links — typed structure suffices. | Case studies only need text, chips, bullets, and links — typed structure suffices. | "+" as English conjunction is non-standard prose; replaced with a serial comma list. |
| Considered for content-without-deploy editability. Rejected because: | Considered for content-without-deploy editability. Rejected for these reasons: | Same fragment fix as in §3.1. |
| Adds API surface, auth, environment variables, build-time fetching complexity. | Adds API surface, auth, environment variables, and build-time fetching complexity. | Added the serial "and" before the final item; without it, "build-time fetching complexity" reads as one more item in apposition rather than the closing item. |
| Common in senior FE portfolios. Rejected because: | Common in senior FE portfolios. Rejected for these reasons: | Same fragment fix as in §3.1. |
| Conceptually off-brand — the DevTools metaphor doesn't want a hero that screams "look at my shader," it wants quiet competence. | Conceptually off-brand: the DevTools metaphor doesn't want a hero that screams "look at my shader"; it wants quiet competence. | Replaced em-dash with colon (the second clause is a gloss, not an aside); replaced the comma splice between two independent clauses with a semicolon. |
| Rejected for v1 because: | Rejected for v1 for these reasons: | Same fragment fix as in §3.1. |
| Audience (EM/recruiter in target markets) is mostly EN-comfortable. | The audience (EMs and recruiters in target markets) is mostly EN-comfortable. | Added the article "The"; expanded the slash-separated pair to "EMs and recruiters" (slash is ambiguous: alias? alternative? both?). |
| Defer; revisit if Arabic-speaking recruiter traffic shows up in analytics. | Defer. Revisit if Arabic-speaking recruiter traffic shows up in analytics. | Two independent imperatives separated by a semicolon read as one merged thought; splitting into two sentences makes both directives equally weighted. |
| Per-action-per-session = visit-tab-Network only grants +10 once per session. | Per-action-per-session means: visiting the Network tab grants +10 XP only once per session. | The original uses "=" as English copula and elides articles. Replaced with explicit "means:"; expanded "visit-tab-Network" (which reads as a code identifier) into the natural phrasing; clarified that "+10" refers to XP. Materially improves LLM parseability. |
| Implementation: track granted reasons in `sessionStorage["hm_xp_granted"]` (NOT `localStorage` — fresh tab = fresh XP grants for re-exploration). On `emitXP(delta, reason)`, check if `reason` is already in the granted set for visit-type reasons. | Implementation: track granted reasons in `sessionStorage["hm_xp_granted"]` (NOT `localStorage` — a fresh tab means fresh XP grants for re-exploration). On `emitXP(delta, reason)`, check whether `reason` is already in the granted set for visit-type reasons. | Replaced "=" used as English copula with "means"; added the article "a"; replaced "check if" with "check whether" — "whether" is the unambiguous conjunction for a yes/no test (LLM-precision: "if" can read as a conditional). |
| Use this template when authoring `Project` entries that won't have full case studies. Each field gets a reasonable mock value derived from the legacy data: | Use this template when authoring `Project` entries that will not have full case studies. Each field receives a reasonable mock value derived from the legacy data: | Expanded contraction "won't" to "will not" (LLM-mode preference for unambiguous tokens in normative statements); replaced informal "gets" with the more precise "receives." |
| `meta.mock: true` initially, with `problem`, `role`, `decisions`, `outcomes` populated as `[ASSUMPTION] …` strings. | Set `meta.mock: true` initially, with `problem`, `role`, `decisions`, and `outcomes` populated as `[ASSUMPTION] …` strings. | The original is a sentence fragment; added the imperative verb "Set" and the serial "and" before the final list item. |
| Hossam fills real content in PRs as he writes them. Each PR flips `meta.mock: false`. | Hossam fills in real content in PRs as he writes them. Each PR flips `meta.mock` to `false`. | "Fills in" is the conventional verb form (fills *out* a form, fills *in* content); "flips … false" is ambiguous code-shorthand — spelled out as "flips … to `false`" to make the assignment direction explicit. |
| Launch gate: all 6 featured projects have `meta.mock: false`. | Launch gate: all 6 featured projects must have `meta.mock: false`. | "Have" reads as a description; "must have" reads as a requirement, which is what a launch gate is. Removes hedging — LLM-precision priority. |
| NFR-A6: Recruiter Mode prints clean. CSS targets: | NFR-A6: Recruiter Mode prints cleanly. CSS targets: | "Prints clean" uses an adjective where an adverb is required (modifying the verb "prints"). |
| Test: macOS Cmd+P preview should produce a clean 1–2-page PDF equivalent of the resume. | Test: on macOS, the Cmd+P preview should produce a clean 1-to-2-page PDF equivalent of the resume. | Added "on" and "the" for grammaticality; replaced "1–2-page" (en-dash inside a hyphenated compound is ambiguous) with the explicit "1-to-2-page." |
| [ASSUMPTION] v1: ship the existing PDF as `/public/hossam-marey-resume.pdf`. Hossam re-designs in matching Obsidian/Lime style as a v1.1 task (PDF authoring is outside the scope of the web build). | [ASSUMPTION] v1: ship the existing PDF as `/public/hossam-marey-resume.pdf`. Hossam redesigns the PDF in the matching Obsidian-and-Lime style as a v1.1 task (PDF authoring is outside the scope of the web build). | "Re-designs" is non-standard ("redesigns" is the conventional spelling); the original lacks an explicit object ("redesigns *what*?") — restored "the PDF"; replaced slash form "Obsidian/Lime" with explicit conjunction. |
| Alternative considered: render `/recruiter` → PDF via Puppeteer at build time. Rejected: adds Puppeteer dep (~250MB), requires Vercel function execution time, and the print stylesheet (§6) gives users a free local PDF via Cmd+P anyway. | Alternative considered: render `/recruiter` to PDF via Puppeteer at build time. Rejected: adds a Puppeteer dependency (roughly 250 MB), requires Vercel function execution time, and the print stylesheet (§6) already gives users a free local PDF via Cmd+P. | Replaced the arrow "→" with "to" in prose; spelled out "dep" → "dependency"; replaced tilde-as-prefix with "roughly"; added the article "a"; replaced sentence-final "anyway" (which hedges) with a tighter construction using "already" earlier in the clause. |
| These are places where the PRD/build differs from `docs/design-system.md`. Each is a deliberate choice with rationale, not an oversight. | These are places where the PRD or the build differs from `docs/design-system.md`. Each is a deliberate choice with rationale, not an oversight. | The slash "PRD/build" is ambiguous (alternative? joint? alias?). Spelled out as "PRD or the build" — the intent is that either source can differ from the design-system doc. |
| Any future departure from `docs/design-system.md` should add a row here with the same shape. | Any future departure from `docs/design-system.md` should be documented as a row in this table with the same shape. | The original elides the agent ("departure" can't "add a row"). Made the construction passive-but-explicit so the directive is clear: the departure should be documented as a row. |
| Project name: `web` (matches `package.json`). [ASSUMPTION: Hossam will rename to `hossam-marey-portfolio` or similar in the Vercel dashboard] | Project name: `web` (matches `package.json`). [ASSUMPTION: Hossam will rename the project to `hossam-marey-portfolio` or similar in the Vercel dashboard.] | Restored the missing object ("rename *what*?"); added the closing period inside the bracketed assumption for consistency with other `[ASSUMPTION]` items in the doc. |
| `vercel.json` is optional. Only add if/when CSP headers, redirects, or rewrites become necessary. Sample for the CSP case: | `vercel.json` is optional. Add it only if and when CSP headers, redirects, or rewrites become necessary. Sample for the CSP case: | Replaced sentence-initial "Only" with a complete imperative ("Add it"); spelled out the "if/when" slash-pair to "if and when." |
| (CSP above is permissive starter — tighten before live by removing `'unsafe-inline'` once specific inline scripts/styles are identified or moved to nonces.) | (The CSP above is a permissive starter — tighten it before going live by removing `'unsafe-inline'` once specific inline scripts and styles are identified or moved to nonces.) | Added the definite article "The" and indefinite "a"; restored the pronoun object ("tighten *it*"); replaced "before live" (telegraphic) with "before going live"; replaced slash "scripts/styles" with "scripts and styles." |
